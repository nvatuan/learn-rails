<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if can_delete? user %>
    | <%= link_to t("users.index.delete_lbl"), user, method: :delete,
                  data: {confirm: t("users.index.del_confirm_text")} %>
  <% end %>
</li>
